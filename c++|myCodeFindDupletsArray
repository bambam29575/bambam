#include <iostream>
using namespace std;

int main(){
    int arr[7] = {1,2,3,4,5,6,3},i , j, duplet = 6, sum;
    for(i = 0; i < 7; i++){
        for(j = i; j < 7; j++){
            if(i == j){
                j++;
            }
            if(arr[i] + arr[j] == duplet){
                cout << arr[i] << " " << arr[j] << endl;
            }
        }
    }
    
    
    return 0;
}
